{
  "name": "@rovacon/voice",
  "version": "0.1.0",
  "description": "Votrax SC-01 style formant synthesis for Rovacon",
  "type": "module",
  "main": "./src/voice/index.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
